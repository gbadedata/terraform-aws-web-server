# Provider binaries — large, re-downloadable with terraform init
**/.terraform/*

# State files — CONTAIN SENSITIVE DATA, never commit
*.tfstate
*.tfstate.*

# Variable values — may contain secrets
*.tfvars
!*.tfvars.example

# Crash logs
crash.log
crash.*.log

# NOTE: .terraform.lock.hcl SHOULD be committed — it pins provider versions
